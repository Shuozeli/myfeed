# myfeed

Personal feed bot that crawls social media via pwright browser automation
and sends updates to Telegram.

## Quick Orientation

```
src/
  main.rs        CLI: run, once, login, list, snapshots, snapshot, dump, events
  config.rs      All settings from env vars. Panics on any required var missing.
  crawler.rs     Runs pwright recipes, parses output into proto::FeedItem with per-site oneof.
  scheduler.rs   Async loop: crawl -> snapshot -> dedup -> telegram -> atom feed.
                 Uses db_blocking() helper for sync diesel calls.
  db.rs          SQLite via diesel. Mutex<SqliteConnection>. All queries in transactions.
                 Tables: feed_items, event_log, crawl_snapshots.
  feed.rs        Generates Atom 1.0 XML from feed_items. No dependencies.
  schema.rs      Auto-generated by diesel CLI. Do not hand-edit.
  telegram.rs    Telegram Bot sendMessage API. HTML formatting with escaping.
  proto.rs       Generated protobuf types (from proto/myfeed.proto).

proto/
  myfeed.proto   FeedItem with oneof site_data for typed per-site payloads.

recipes/
  explore/       HTML structure discovery scripts (run before writing feed recipes).
  actions/       Follow/join action recipes (follow-linkedin, follow-x, join-reddit).
  <site>-feed.yaml  Feed extraction recipes. Output JSON array via `items` key.

prompts/         Prompt templates for AI agent digests (daily-digest, trending-topics, tech-radar).
migrations/      Diesel SQL migrations. Run via embedded migrations at startup.
docs/            Architecture, design, codelabs, agent-digest-guide, design-comparison.
docs/designs/    Design docs (prefixed YYMMDD-).
docs/private/    Gitignored. Site exploration notes, feed snapshots, HTML dumps.
```

## How the Crawl Pipeline Works

1. `scheduler.rs` timer fires (default 30min)
2. Cleanup event log entries older than 30 days
3. For each site in `ENABLED_SITES` (with retry: 3 attempts, 5s delay):
   - Opens a new Chrome tab via `Browser::connect_http()` + `new_tab("about:blank")`
   - Runs the recipe via `pwright_script::executor::execute()`
   - `crawler.rs` parses output into `proto::FeedItem` with per-site oneof data
   - Saves full crawl snapshot to `crawl_snapshots` table
   - Atomic dedup via `INSERT OR IGNORE` into `feed_items`
   - Keyword filter: only matching items go to Telegram (all saved to DB)
   - Digest mode: batch into one message, or one message per item
   - Tab always closed (even on error, via `crawl_with_page` separation)
4. After all sites: generates Atom feed if `FEED_OUTPUT_PATH` is set
5. Events logged: `crawl_start`, `crawl_complete`, `crawl_error`

## AI Agent Integration

Agents (Claude Code, Gemini CLI, etc.) can read feed data via `myfeed dump`:

```bash
myfeed dump --hours 24 --compact    # Step 1: scan titles (~10 tokens/item)
myfeed dump --ids 42,55,78          # Step 2: read details for selected items
```

See `docs/agent-digest-guide.md` for the full workflow. Prompt templates
in `prompts/` (daily-digest, trending-topics, tech-radar).

## Key Dependencies

- `pwright-bridge` -- Chrome CDP connection, Browser, TabHandle, Page
- `pwright-script` -- Recipe parser, executor, VecSink for capturing output
- `pwright-cdp` -- Low-level CDP types (imported transitively)
- `diesel` -- SQLite ORM with migrations
- `reqwest` -- HTTP client for Telegram API
- `tokio` -- Async runtime
- `clap` -- CLI argument parsing

All pwright crates are git dependencies from `https://github.com/shuozeli/pwright.git`.

## Build & Test

```bash
cargo build                           # Build
cargo build --release                 # Release build
./target/release/myfeed once          # Test single crawl (needs Chrome + .env)
./target/release/myfeed events -l 10  # Check event log
```

Unit tests exist in config.rs, crawler.rs, feed.rs, and telegram.rs (18 tests total).
CI runs `cargo test` on every push. Integration testing requires Chrome with remote
debugging + valid .env config.

## Rules

### Architecture
- **Browser-only crawling.** Do NOT add API-based crawlers. Everything goes through
  pwright recipes. This is a design principle, not a temporary limitation.
- **Recipe-based extraction.** Site-specific DOM logic lives in YAML recipes, not Rust.
  Adding a new site should require zero Rust code changes (except adding its login URL
  to the `Login` command match in `main.rs`).
- **Stateless tabs.** Each crawl opens a fresh tab and closes it when done.
  Do NOT pool tabs or reuse sessions across crawls.

### Code
- All DB queries MUST be wrapped in diesel transactions (`conn.transaction(|conn| {...})`)
- Config values MUST NOT have defaults -- panic on missing env vars
- Do NOT commit `.env` or `*.db` files
- Do NOT hand-edit `src/schema.rs` -- it's generated by `diesel print-schema`
- Recipes must output items as JSON array with keys: `id`, `title`, `url`, `preview`

### Design Docs
- Design docs go in `docs/designs/` with filename prefix `YYMMDD-` (e.g., `260323-atom-feed-output.md`)
- Write the design doc BEFORE implementing. Get alignment first.

### Git
- Do NOT commit or push unless explicitly asked
- Do NOT amend commits unless explicitly asked

## Adding a New Site

1. Create `recipes/explore/<site>-explore.yaml` (copy an existing one, change selectors)
2. Run it via pwright CLI to understand the DOM: `pwright script run recipes/explore/<site>-explore.yaml`
3. Create `recipes/<site>-feed.yaml` with JS that extracts `[{id, title, url, preview}]`
4. Add the site name to `ENABLED_SITES` in `.env`
5. Add login URL to `main.rs` `Command::Login` match arm

## Common Modifications

| Task | Where to change |
|------|----------------|
| Adjust crawl interval | `CRAWL_INTERVAL_SECS` in `.env` |
| Change Telegram message format | `telegram.rs` `send_feed_item()` |
| Add a new site | See "Adding a New Site" above |
| Fix broken selectors | Edit the site's `recipes/<site>-feed.yaml` |
| Add a new CLI command | `main.rs` `Command` enum + match arm |
| Change DB schema | New migration in `migrations/`, then `diesel print-schema > src/schema.rs` |
| Add keyword filtering | `crawler.rs` `parse_outputs()` or new filter step in `scheduler.rs` |
